<h1>Editing feed</h1>

<%= error_messages_for :feed %>

<% form_for(:feed, :url => feed_path(@feed), :html => { :method => :put }) do |f| %>
  <p>
    <%= submit_tag "Update" %>
  </p>
<% end %>

<%= link_to 'Show', feed_path(@feed) %> |
<%= link_to 'Back', feeds_path %>