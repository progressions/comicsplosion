<% @favorites.each do |favorite| %>
  <% @page = favorite.page %>
  <%= link_to image_tag(url_for_file_column('page','image','thumb')), comic_path(:id => @page.comic.alias, :page => @page.locate)  %>
<% end %>